services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: moto
      POSTGRES_PASSWORD: moto
      POSTGRES_DB: motocompare
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U moto -d motocompare"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
